import { colors } from "../../utils/ui-data";
import RadioFilter from "../ui/RadioFilter";

const Colors = () => {
  return <RadioFilter heading={"Colors"} filterData={colors} />;
};

export default Colors;
